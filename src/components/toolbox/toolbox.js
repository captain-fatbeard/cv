import React from "react";
import "./toolbox.scss"

export default class Toolbox extends React.Component {
  render() {
    return (
        <div className="toolbox">
            toolbox
        </div>
    );
  }
}